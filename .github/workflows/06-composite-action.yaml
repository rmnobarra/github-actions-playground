name: 'Composite Action Demo'

on: [push]

jobs:
  demo:
    name: 'Exemplo Composite Action'
    runs-on: ubuntu-latest

    steps:

      - name: Checkout code
        uses: actions/checkout@v3

      - name: Usar Composite Action
        uses: ./actions/my-composite-action
        with:
          name: 'GitHub User'

      - name: Exibir saída
        run: |
          echo "Workflow completo!"
